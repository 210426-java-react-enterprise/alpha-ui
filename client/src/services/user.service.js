import {alphacastAuthenticatedClient} from "../utilities/alphacastClients";
import authHeader from "./auth-header";

class UserService{
    //TODO have actual calls to Alphacast API here utilize authHeader() to grab the header from the JWT and pass along into the request
}

export default new UserService();